import { StreamChat } from 'stream-chat';
window.StreamChat = StreamChat;

// This file serves as the entry point for Vite to bundle the StreamChat library for browser usage because browser needed specific version of stream-chat as opposed to the node or react version.